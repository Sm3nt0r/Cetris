cmake_minimum_required(VERSION 3.15)
project(tetris C)

set(CMAKE_C_STANDARD 11)

set(RAYLIB_PATH "F:/Tetris_project/raylib-5.5")

include_directories(${RAYLIB_PATH}/include)
link_directories(${RAYLIB_PATH}/lib)

add_executable(tetris src/main.c
        src/main.c
        inc/render.h
        inc/game.h
        src/game.c
        src/render.c
)

target_include_directories(tetris PRIVATE inc)

target_link_libraries(tetris raylib opengl32 gdi32 winmm)
